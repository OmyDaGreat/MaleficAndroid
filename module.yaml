product: android/app

dependencies:
  - $compose.foundation
  - $compose.material3
  # Compose integration with Android activities
  - androidx.activity:activity-compose:1.7.2

settings:
  android:
    namespace: "xyz.malefic.android"
    signing: enabled
  compose:
    enabled: true
    experimental:
      hotReload: enabled
